public class IsomorphicStrings{
	
	public boolean isIsomorphic (String first, String second){

	  Map<Character, Character> charMap = new HashMap<Character, Character>();
	  if(null == first || null == second){
	    return Boolean.FALSE;
	  }
	  
	  if(first.length() != second.length()){
	    return Boolean.FALSE;
	  }
	  
	  for (int counter = 0; counter< first.length(); counter++){
	    if(charMap.containsKey(first.charAt(counter))){
	      if(second.charAt(counter) != charMap.get(first.charAt(counter)))
	          return Boolean.FALSE;
	    } else if (charMap.containsValue(second.charAt(counter))){
	          return Boolean.FALSE;
	    } else charMap.put(first.charAt(counter),second.charAt(counter));
	    
	  }// end for
	  
	  return Boolean.TRUE;
	}
	
	public static void main (String[] args){
		IsomorphicStrings objIS = new IsomorphicStrings();
		System.out.println(objIS.isIsomorphic("add", "egg"));
		System.out.println(objIS.isIsomorphic("bar", "foo"));
	}

}
